steps:
  - uses: actions/checkout@v2
    name: Check out code

uses: mr-smithers-excellent/docker-build-push@v5
with:
  image: nimares/mysite-web
  registry: docker.io
  username: ${{ secrets.DOCKER_USERNAME }}
  password: ${{ secrets.DOCKER_PASSWORD }}